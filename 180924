import math

#19 variant

d = float(input("Введите высоту в метрах с которой упадет объект: "))
a = 9.8
speed = math.sqrt(2*a**2*d)

print(f"Скорость объекта при столкновении с землей составит {speed} метров в секунду")
